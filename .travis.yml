language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install

#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "flyingzl" --{GitHub账户名称}
  - git config user.email "flyingzl@gmail.com" --{Github账户邮箱}
  - git add .
  - git commit -m "fea:更新文章"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" gh-pages:gh-pages
# E: Build LifeCycle

branches:
  only:
    - master
env:
 global:
   - GH_REF: github.com/flyingzl/flyingzl.github.io.git --{仓库地址}